---
const { inputType, inputName, labelText, ...rest } = Astro.props;
---

<div class="flex flex-col gap-1">
  <label class="text-gray-600" for={inputName}>{labelText}</label>
  <input id={inputName} class="bg-gray-100 rounded-lg p-2" type={inputType} name={inputName} {...rest} />
</div>
